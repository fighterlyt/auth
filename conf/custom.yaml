mysql: # mysql配置
  db: auth        # 数据库名
  user: root             # 用户名
  pass: 123            # 密码
  host: 127.0.0.1            # 地址
  port: 3306            # 端口
  maxIdle: 100
  maxOpen: 300
  maxLife: 30m
http:
  port: 8001